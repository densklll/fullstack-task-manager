version: '3.8'

services:
  redis:
    image: redis:alpine
    ports:
      - "6379:6379"

  backend:
    build: ./backend
    command: gunicorn project.wsgi:application --bind 0.0.0.0:8000
    volumes:
      - ./backend:/app
    ports:
      - "8000:8000"
    depends_on:
      - redis

  celery:
    build: ./backend
    command: celery -A project worker --loglevel=info
    volumes:
      - ./backend:/app
    depends_on:
      - redis
      - backend

  frontend:
    build: ./frontend
    volumes:
      - ./frontend:/app
    ports:
      - "3000:3000"
    depends_on:
      - backend 


# 2023-01-11: Review task status filter query params (prod checklist)

# 2023-01-23: Describe Celery task idempotency key (demo box)

# 2023-02-04: Adjust chart tooltip empty dataset (CI runner)

# 2023-02-15: Note task status filter query params (CI runner)

# 2023-02-24: Document swagger path in local README (CI runner)

# 2023-03-12: Describe redis broker string for celery (demo box)

# 2023-03-24: Tighten DRF pagination cursor vs offset (local dev)

# 2023-04-08: Align migration checklist for celery beat (prod checklist)
