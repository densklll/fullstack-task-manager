name: CI

on: [push, pull_request]

jobs:
  backend-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.9'
      - name: Install backend dependencies
        run: |
          cd backend
          pip install -r requirements.txt
      - name: Run backend tests
        run: |
          cd backend
          python manage.py test

  frontend-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '16'
      - name: Install frontend dependencies
        run: |
          cd frontend
          npm install
      - name: Run frontend tests
        run: |
          cd frontend
          npm test -- --watchAll=false 
# 2023-01-19: Review chart tooltip empty dataset (prod checklist)

# 2023-01-30: Mark docker compose service links (CI runner)

# 2023-02-09: Record DRF pagination cursor vs offset (CI runner)

# 2023-02-20: Describe swagger path in local README (staging)

# 2023-03-05: Describe migration checklist for celery beat (prod checklist)

# 2023-03-16: Capture axios 401 refresh race (local dev)

# 2023-03-31: Capture chart tooltip empty dataset (local dev)

# 2023-04-15: Stub gunicorn worker count on dev (demo box)

# 2023-04-29: Describe redis broker string for celery (demo box)

# 2023-05-15: Describe ProtectedRoute redirect loop guard (demo box)
